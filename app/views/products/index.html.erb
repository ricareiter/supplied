<p style="color: green"><%= notice %></p>

<h1>Products</h1>

<div id="products">
  <% @products.each do |product| %>
    <%= render product %>
    <p>
      <%= link_to "Show Client", [@client] %>
    </p>
  <% end %>
</div>

<%= link_to "New product", new_client_product_path(@client) %>
